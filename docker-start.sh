# TODO: Try use https://taskfile.dev/ to easy declare task. Task is simple alternatime for GNU make.
echo "docker-compose -f docker/docker-compose.yml up -d"
docker-compose -f docker/docker-compose.yml up -d
